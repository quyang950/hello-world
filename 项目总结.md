# 测试用例标准化和去重解决方案

## 问题分析

您的问题主要包含两个方面：

1. **数据库组件名称不规范** - 例如"og"需要标准化为"OpenGauss"
2. **测试用例重复** - 相同的测试步骤和预期结果，但用例名称包含不同的具体系统名称

## 解决方案设计

### 核心思路

1. **配置化标准化映射** - 通过配置文件定义数据库名称的标准化规则
2. **系统名称抽象化** - 将具体的系统名称（如"订单系统"、"用户管理系统"）统一替换为占位符"【xxx】"
3. **多层次去重算法** - 结合哈希值快速匹配和相似度计算进行精确去重
4. **灵活的相似度控制** - 支持自定义相似度阈值，控制去重的严格程度

### 技术实现

#### 1. 标准化处理
- **数据库名称映射**: 支持不区分大小写的精确匹配替换
- **中英文混合处理**: 使用前瞻后顾断言确保准确匹配
- **系统名称抽象化**: 基于正则表达式的模式匹配

#### 2. 去重算法
- **快速哈希匹配**: 基于测试步骤和预期结果生成MD5哈希值
- **相似度计算**: 使用SequenceMatcher计算文本相似度
- **加权评分**: 测试步骤权重70%，预期结果权重30%

#### 3. 配置管理
- **JSON配置文件**: 易于维护和扩展
- **运行时加载**: 支持动态配置更新
- **默认配置**: 内置常用数据库名称映射

## 文件结构

```
项目文件/
├── test_case_processor.py    # 核心处理类
├── cli_processor.py          # 命令行工具
├── config.json              # 配置文件
├── test_cases_example.json  # 示例数据
├── 使用说明.md              # 详细使用说明
└── 项目总结.md              # 本文档
```

## 使用方式

### 方式1: 直接运行（推荐新手）
```bash
python3 test_case_processor.py
```

### 方式2: 命令行工具（推荐批量处理）
```bash
# 基本用法
python3 cli_processor.py -i your_test_cases.json

# 完整参数
python3 cli_processor.py -i input.json -o output.json -c config.json -t 0.85 --show-duplicates -v
```

### 方式3: Python代码集成
```python
from test_case_processor import TestCaseProcessor

processor = TestCaseProcessor('config.json')
result = processor.process_test_cases('input.json', 'output.json', 0.85)
processor.print_summary(result)
```

## 处理效果演示

### 标准化效果
- **输入**: `"订单系统数据库og连接测试"`
- **输出**: `"订单系统数据库OpenGauss连接测试"`

### 抽象化效果
- **输入**: `"订单系统数据库连接测试"`、`"用户管理系统数据库连接测试"`
- **抽象化**: 都变成 `"【xxx】数据库连接测试"`
- **结果**: 识别为重复用例并去重

### 实际处理结果
使用示例数据测试：
- 原始用例数量: 5个
- 去重后数量: 3个  
- 重复用例数量: 2个
- 去重率: 40%

## 配置自定义

### 数据库名称映射
在`config.json`中添加或修改映射规则：
```json
{
  "database_name_mapping": {
    "og": "OpenGauss",
    "your_db_alias": "StandardName"
  }
}
```

### 系统名称模式
添加需要抽象化的系统名称：
```json
{
  "system_name_patterns": [
    "订单系统",
    "您的系统名称"
  ]
}
```

### 相似度阈值
调整去重的严格程度：
- `0.9+`: 非常严格，只去除几乎完全相同的用例
- `0.85`: 推荐值，平衡准确性和去重效果
- `0.7-`: 较宽松，可能误删不同的用例

## 扩展能力

### 1. 支持新的数据库类型
只需在配置文件中添加映射关系，无需修改代码。

### 2. 自定义相似度算法
可继承`TestCaseProcessor`类重写`calculate_similarity`方法。

### 3. 批量处理
支持处理大型JSON文件，内存使用优化。

### 4. 数据备份
自动创建输入文件备份，避免数据丢失。

## 使用建议

1. **小批量测试**: 先用少量数据测试配置是否正确
2. **阈值调优**: 根据实际数据特点调整相似度阈值
3. **配置维护**: 定期更新数据库映射和系统模式
4. **结果验证**: 检查去重结果，确保没有误删重要用例
5. **数据备份**: 处理重要数据前先备份原始文件

## 解决的核心问题

✅ **标准化数据库名称** - 统一不规范的数据库组件名称  
✅ **智能去重** - 识别内容相同但名称不同的重复用例  
✅ **可配置化** - 支持自定义映射规则和处理参数  
✅ **高效处理** - 支持大批量数据处理  
✅ **易于使用** - 提供多种使用方式适应不同场景

这个解决方案能够有效解决您提到的测试用例标准化和去重问题，提高测试用例库的质量和维护效率。
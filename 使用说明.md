# 测试用例标准化和去重工具使用说明

## 功能概述

本工具主要解决两个问题：

1. **数据库组件名称标准化** - 将非标准的数据库名称（如"og"）统一替换为标准名称（如"OpenGauss"）
2. **测试用例去重** - 识别并去除内容相似的重复测试用例，支持将具体的系统名称抽象化

## 文件结构

```
.
├── test_case_processor.py    # 主处理脚本
├── config.json              # 配置文件
├── test_cases_example.json  # 示例测试用例数据
├── 使用说明.md              # 本文档
└── processed_test_cases.json # 处理结果（运行后生成）
```

## 配置文件说明 (config.json)

### 数据库名称映射 (database_name_mapping)
定义非标准数据库名称到标准名称的映射关系：

```json
{
  "database_name_mapping": {
    "og": "OpenGauss",           // og -> OpenGauss
    "mysql": "MySQL",            // mysql -> MySQL
    "pg": "PostgreSQL"           // pg -> PostgreSQL
  }
}
```

### 系统名称模式 (system_name_patterns)
定义需要抽象化的系统名称模式：

```json
{
  "system_name_patterns": [
    "订单系统",
    "用户管理系统",
    "库存管理系统"
  ]
}
```

### 其他配置参数
- `similarity_threshold`: 相似度阈值（0-1），默认0.85
- `replacement_placeholder`: 系统名称替换占位符，默认"【xxx】"

## 测试用例数据格式

输入的测试用例JSON文件应包含以下字段：

```json
[
  {
    "id": 1,
    "case_name": "订单系统数据库og连接测试",
    "description": "验证订单系统与og数据库的连接功能",
    "test_steps": [
      "1. 启动订单系统服务",
      "2. 配置og数据库连接参数",
      "3. 测试数据库连接",
      "4. 验证连接状态"
    ],
    "expected_result": "系统能够成功连接到og数据库，连接状态显示正常"
  }
]
```

## 使用方法

### 1. 基本使用

```bash
python test_case_processor.py
```

### 2. 在Python代码中使用

```python
from test_case_processor import TestCaseProcessor

# 使用默认配置
processor = TestCaseProcessor()

# 使用自定义配置文件
processor = TestCaseProcessor('my_config.json')

# 处理测试用例
result = processor.process_test_cases(
    input_file='my_test_cases.json',
    output_file='processed_result.json',
    similarity_threshold=0.85
)

# 打印处理结果摘要
processor.print_summary(result)
```

### 3. 命令行参数（可扩展）

可以通过修改脚本底部的主函数来支持命令行参数：

```python
import argparse

if __name__ == "__main__":
    parser = argparse.ArgumentParser(description='测试用例标准化和去重工具')
    parser.add_argument('--input', '-i', required=True, help='输入文件路径')
    parser.add_argument('--output', '-o', help='输出文件路径')
    parser.add_argument('--config', '-c', default='config.json', help='配置文件路径')
    parser.add_argument('--threshold', '-t', type=float, default=0.85, help='相似度阈值')
    
    args = parser.parse_args()
    
    processor = TestCaseProcessor(args.config)
    result = processor.process_test_cases(
        input_file=args.input,
        output_file=args.output,
        similarity_threshold=args.threshold
    )
    processor.print_summary(result)
```

## 处理流程

1. **加载配置** - 读取config.json中的配置参数
2. **读取测试用例** - 解析输入的JSON文件
3. **标准化处理** - 应用数据库名称映射和系统名称抽象化
4. **相似度计算** - 计算测试用例间的相似度
5. **去重处理** - 移除重复的测试用例
6. **生成结果** - 输出去重后的用例和统计信息

## 输出结果说明

处理完成后会生成包含以下内容的结果文件：

```json
{
  "summary": {
    "original_count": 5,        // 原始用例数量
    "unique_count": 3,          // 去重后数量
    "duplicate_count": 2,       // 重复用例数量
    "similarity_threshold": 0.85 // 使用的相似度阈值
  },
  "unique_cases": [...],        // 去重后的用例列表
  "duplicate_cases": [...]      // 被标识为重复的用例列表
}
```

每个处理后的测试用例会添加以下字段：
- `generalized_name`: 抽象化后的用例名称
- `content_hash`: 内容哈希值，用于快速去重

## 自定义扩展

### 1. 添加新的数据库映射
在config.json的database_name_mapping中添加新的映射关系。

### 2. 扩展系统名称模式
在config.json的system_name_patterns中添加新的模式。

### 3. 调整相似度算法
可以修改`calculate_similarity`方法来调整相似度计算逻辑。

### 4. 自定义处理逻辑
可以继承TestCaseProcessor类并重写相关方法。

## 注意事项

1. **编码格式**: 确保输入文件使用UTF-8编码
2. **文件路径**: 支持相对路径和绝对路径
3. **配置验证**: 建议在处理大量数据前先用小样本测试配置是否正确
4. **备份数据**: 处理前建议备份原始数据
5. **内存使用**: 大文件处理时注意内存使用情况

## 示例场景

### 场景1：标准化数据库名称
输入：`"订单系统og数据库连接测试"`
输出：`"订单系统OpenGauss数据库连接测试"`

### 场景2：系统名称抽象化
输入：`"订单系统数据库连接测试"`、`"用户管理系统数据库连接测试"`
抽象化后：都变成`"【xxx】数据库连接测试"`

### 场景3：去重处理
如果两个用例的测试步骤和预期结果相似度超过阈值，则被认定为重复用例。

## 故障排除

1. **配置文件错误**: 检查JSON格式是否正确
2. **文件不存在**: 确认输入文件路径是否正确
3. **编码错误**: 确保文件使用UTF-8编码
4. **内存不足**: 考虑分批处理大文件
5. **相似度阈值**: 调整threshold参数来改变去重严格程度